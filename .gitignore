# Build outputs
bin/
obj/
artifacts/
# Local MSIX dev signing — see Desktop/HermesDesktop/packaging/README.md and scripts/new-msix-dev-cert.ps1.
# CI uses an ephemeral cert (.github/workflows/ci-msix.yml); do not commit .pfx material.
Desktop/HermesDesktop/packaging/dev-msix.pfx
*.dll
*.exe
*.xbf
*.pri
*.msix
*.msixbundle

# Visual Studio
.vs/
*.user
*.suo

# Claude Code local settings (machine-specific paths and permissions)
.claude/settings.local.json

# Cursor IDE local state (machine-specific paths + transcript refs)
# These contain absolute user paths like C:\Users\<name>\... and must not be committed.
.cursor/hooks/state/

# VS Code workspace settings — machine-specific launch/tasks configuration.
.vscode/

# OS
Thumbs.db
.DS_Store

# Logs
*.log

# Config (optional - keep template)
# config.yaml
.aider*

# Local dotnet SDK / tool payloads (not repo source)
.dotnet/
.dotnet-cli/

# Nested clone kept alongside the repo for reference (do not vendor)
hermes-agent-repo/

# Ad-hoc logs / tree dumps at repo root
/err.txt
/out.txt
/repo-tree.txt

# One-off documentation / analysis exports (regenerate locally if needed)
/hermes-forensic-report.md
/hermes-code-patterns.md
/hermes-complete-docs.md
