<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HermesDesktop.Views.MemoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid Padding="32,24,32,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Spacing="4" Margin="0,0,0,16">
            <TextBlock Text="Memory" Style="{StaticResource PageEyebrowTextStyle}"/>
            <TextBlock Text="Memories and Project Rules" FontSize="22" FontWeight="SemiBold"/>
            <TextBlock Text="Persistent memory files and project-specific conventions"
                       FontSize="13" Foreground="{StaticResource AppTextSecondaryBrush}"/>
        </StackPanel>

        <!-- Tab bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="4" Margin="0,0,0,16">
            <Button x:Name="TabMemories" Content="Memories" Click="Tab_Click" Tag="memories"
                    Background="{StaticResource AppAccentGradientBrush}" Foreground="#16110D" FontSize="13" Padding="16,8" CornerRadius="8"/>
            <Button x:Name="TabProject" Content="Project" Click="Tab_Click" Tag="project"
                    Background="{StaticResource AppInsetBrush}" Foreground="{StaticResource AppTextSecondaryBrush}" FontSize="13" Padding="16,8" CornerRadius="8"
                    BorderBrush="{StaticResource AppStrokeBrush}"/>
        </StackPanel>

        <!-- ═══════════════════════════════════════════════ -->
        <!-- MEMORIES TAB                                     -->
        <!-- ═══════════════════════════════════════════════ -->
        <Grid x:Name="MemoriesContent" Grid.Row="2" Visibility="Visible" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" MinWidth="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Memory list -->
            <Border Style="{StaticResource SurfaceCardStyle}" Padding="0">
                <Grid RowDefinitions="Auto,*">
                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="16,12">
                        <TextBlock Text="Memory Files" FontSize="16" FontWeight="SemiBold"
                                   Foreground="{StaticResource AppAccentTextBrush}"/>
                        <Border Background="#3A3A00" CornerRadius="8" Padding="10,4" VerticalAlignment="Center">
                            <TextBlock x:Name="MemoryCountBadge" Text="0" FontSize="12"
                                       Foreground="{StaticResource AppAccentTextBrush}"/>
                        </Border>
                    </StackPanel>

                    <ListView Grid.Row="1" x:Name="MemoryList" Background="Transparent" Padding="8"
                              SelectionChanged="MemoryList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,Auto" Margin="0,6">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Filename}" FontSize="13" FontWeight="SemiBold"
                                                   Foreground="{StaticResource AppAccentTextBrush}"/>
                                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0">
                                            <Border Background="{Binding TypeColor}" CornerRadius="4" Padding="6,2">
                                                <TextBlock Text="{Binding Type}" FontSize="10" Foreground="White"/>
                                            </Border>
                                            <TextBlock Text="{Binding Age}" FontSize="11"
                                                       Foreground="{Binding AgeBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <TextBlock x:Name="EmptyState" Grid.Row="1" Visibility="Collapsed"
                               Text="No memories yet.&#x0a;Memories are created automatically during conversations."
                               Foreground="{StaticResource AppTextSecondaryBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               TextAlignment="Center" FontSize="13" Opacity="0.7"/>
                </Grid>
            </Border>

            <!-- Right: Memory preview -->
            <Border Grid.Column="1" Style="{StaticResource SurfaceCardStyle}">
                <Grid RowDefinitions="Auto,*">
                    <TextBlock x:Name="MemoryPreviewTitle" Text="Select a memory to preview" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource AppAccentTextBrush}" Margin="0,0,0,12"/>
                    <ScrollViewer Grid.Row="1">
                        <TextBlock x:Name="MemoryPreviewText" FontSize="13" FontFamily="Consolas" TextWrapping="Wrap"
                                   Foreground="{StaticResource AppTextSecondaryBrush}" IsTextSelectionEnabled="True"
                                   Text="Click a memory file from the list to see its content here."/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════════════ -->
        <!-- PROJECT TAB                                      -->
        <!-- ═══════════════════════════════════════════════ -->
        <Grid x:Name="ProjectContent" Grid.Row="2" Visibility="Collapsed" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: AGENTS.md editor -->
            <Border Style="{StaticResource SurfaceCardStyle}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Project Rules (AGENTS.md)" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource AppAccentTextBrush}"/>
                    <TextBlock Text="Define coding conventions, architecture patterns, and project-specific rules for the agent."
                               FontSize="12" Foreground="{StaticResource AppTextSecondaryBrush}"/>
                    <TextBox x:Name="AgentsEditor" AcceptsReturn="True" TextWrapping="Wrap"
                             MinHeight="300" MaxHeight="500" FontSize="12" FontFamily="Consolas"
                             Background="{StaticResource AppInsetBrush}" BorderBrush="{StaticResource AppStrokeBrush}"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Save Project Rules" Click="SaveAgents_Click" FontSize="12" Padding="16,8"
                                Background="{StaticResource AppAccentGradientBrush}" Foreground="#16110D"/>
                        <TextBlock x:Name="SaveStatus" Text="" FontSize="12" VerticalAlignment="Center"
                                   Foreground="{StaticResource AppTextSecondaryBrush}" Opacity="0.7"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Right: Info -->
            <Border Grid.Column="1" Style="{StaticResource SurfaceCardStyle}">
                <StackPanel Spacing="12">
                    <TextBlock Text="About Project Rules" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource AppAccentTextBrush}"/>
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource AppTextSecondaryBrush}"
                               Text="AGENTS.md defines conventions and rules the agent follows when working on your project. These are injected into the system prompt automatically."/>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource AppTextSecondaryBrush}" Opacity="0.8"
                               Text="  - Coding style and naming conventions&#x0a;  - Architecture patterns to follow&#x0a;  - File organization rules&#x0a;  - Testing requirements&#x0a;  - Preferred libraries and frameworks"/>
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource AppTextSecondaryBrush}"
                               Text="Changes are saved to disk and take effect on the next conversation turn."/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
